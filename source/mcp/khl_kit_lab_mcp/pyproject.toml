[project]
name = "khl-kit-lab-mcp"
version = "0.5.0"
description = "Local MCP adapter for the KHL Omniverse Kit laboratory."
requires-python = ">=3.12,<3.13"
dependencies = [
    "mcp==2.1.1",
]

[tool.ruff]
line-length = 100
target-version = "py312"
